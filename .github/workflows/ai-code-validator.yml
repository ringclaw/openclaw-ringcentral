name: AI Code Validator

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  ai-code-validate:
    name: AI Code Quality Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies for open-code-review
        run: npm install web-tree-sitter@0.22.6 tree-sitter-wasms

      - name: AI Code Validator
        uses: raye-deng/open-code-review@v1
        with:
          threshold: 70
          fail-on-low-score: 'false'
        env:
          NODE_PATH: ${{ github.workspace }}/node_modules
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

